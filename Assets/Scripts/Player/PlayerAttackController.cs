using UnityEngine;

public class PlayerAttackController : MonoBehaviour
{
    public float attackTimer;

    void Start()
    {

    }

    void Attack()
    {
    }

    void FixedUpdate()
    {
        // attackTimer += Time.deltaTime;
        // if (attackTimer > CombateStats.attackSpeed)
        // {
        //     Attack();
        //     attackTimer = 0f;
        // }
    }
}
